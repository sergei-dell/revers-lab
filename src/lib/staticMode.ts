// СТАТИЧЕСКАЯ СБОРКА — та, что лежит на GitHub Pages: там нет сервера, а
// значит, нет ни базы, ни ключа, ни AI-описания сцены. Один и тот же код
// собирается двумя способами, и этот признак говорит, каким именно.
//
// Раньше статическая версия жила отдельной веткой и отставала от рабочей
// на семь правок. Теперь она собирается из той же ветки, с этим флагом.
export const STATIC_BUILD = process.env.NEXT_PUBLIC_STATIC === "1";
